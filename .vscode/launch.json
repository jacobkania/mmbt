{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch",
      "type": "go",
      "request": "launch",
      "mode": "debug",
      "program": "${workspaceFolder}",
      "envFile": ["${workspaceFolder}/.env", "${workspaceFolder}/.env.local"],
      "args": []
    }
  ]
}
